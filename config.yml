version: 2.1
jobs:
  #build:
    #...
  deploy:
    machine:
      enabled: true
    steps:
      - add_ssh_keys:
          fingerprints:
            - "ae:cb:17:39:c0:f1:a6:ca:1c:79:f3:82:97:a5:d8:57"
      - run:
          name: Deploy Over SSH
          command: |
             ssh $SSH_USER@$SSH_HOST "cd hello-circleci && git pull && echo hello " 

workflows:
  version: 2
  build-and-deploy:
    jobs:
      #- build
      - deploy:
          #requires:
          #  - build # only deploy once build job has completed
          filters:
            branches:
              only: master # only deploy on the master branch