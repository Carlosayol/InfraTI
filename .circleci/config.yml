version: 2
jobs:
  Development:
    docker:
      - image: google/cloud-sdk
    environment:
      - PROJECT_NAME: "infraestructura-TI"
      - GOOGLE_PROJECT_ID: "infraestructura-ti-286120"
      - GOOGLE_COMPUTE_ZONE: "us-central1-a"
      - VM_NAME: "nfs"
    steps:
      - checkout
      - add_ssh_keys:
          fingerprints:
           - "c1:9c:8a:41:1a:38:42:16:b3:d1:1e:48:ac:c6:30:4d"
      - run: 
          name: Setup Google Cloud SDK
          command: |
            apt-get install -qq -y gettext
            echo $GCLOUD_SERVICE_KEY > ${HOME}/gcloud-service-key.json
            gcloud auth activate-service-account --key-file=${HOME}/gcloud-service-key.json	
            gcloud beta compute ssh --zone ${GOOGLE_COMPUTE_ZONE} ${VM_NAME} --project ${GOOGLE_PROJECT_ID}
      - run: 
          name: Test
          command: |
            cd ${HOME}/hola 
            
  
workflows:
  version: 2
  build:
    jobs:
      - Development:
          filters:
            branches:
              only: master
          
